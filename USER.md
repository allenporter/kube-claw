# User Preferences

- Prefer clean, documented code.
- Always run tests after changes.
- **Workflow**: Never commit directly to the `main` branch. Always create a new branch, push changes, and open a Pull Request (PR) for review. Use the `gh` CLI for PR management.
- **Communication**: Monitor PR comments for feedback and iterate on the PR until approved and merged.
